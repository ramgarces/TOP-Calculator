let firstNum;
let nextNum;
let operator;

function add(first, next) {
    return first + next;
}

function subtract(first, next) {
    return first - next;
}

function multiply(first, next) {
    return first * next;
}

function multiply(first, next) {
    return first * next;
}